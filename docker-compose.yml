version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "8889:8889"
    restart: always

  nginx:
    build: ./nginx
    ports:
      - "8080:8080"
    depends_on:
      - backend
    restart: always
